export interface ThemeType {
  name: string;
  colors: {
    color_long_round: string;
    color_short_round: string;
    color_focus_round: string;
    color_background: string;
    color_background_light: string;
    color_background_lightest: string;
    color_foreground: string;
    color_foreground_darker: string;
    color_foreground_darkest: string;
    color_accent: string;
  };
}

